// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: "AIzaSyCxZDieORN-vMn2eu82-vOBgX7WHe9rql8",
    authDomain: "weatherapi2-220c0.firebaseapp.com",
    databaseURL: "https://weatherapi2-220c0.firebaseio.com",
    projectId: "weatherapi2-220c0",
    storageBucket: "weatherapi2-220c0.appspot.com",
    messagingSenderId: "506749230820"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
